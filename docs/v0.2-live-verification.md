# v0.2 — THE MODEL SEAM: live verification transcript

Worker: https://cell-cascade.casey-digennaro.workers.dev
Date: 2026-08-25 (AKDT) · commit follows this file · all calls below are live HTTP against the deployed worker.

## Environment

- `MODEL_BASE_URL = https://api.z.ai/api/coding/paas/v4` (worker secret — the fleet pattern, openai-compatible)
- `MODEL_KEY = <zai coding-plan key>` (worker secret)
- `MODEL_TIMEOUT_MS = 60000` (env override of the 20s default; glm-4.5-flash is a reasoning model — see honest limits)
- No per-token prices configured → `cost_estimate_usd: null` on every log (honest: tokens logged, cost not guessed).

## Phase 1 — the honest boundary, BEFORE secrets were set

```
GET  /                                     → model_seam.status: "unconfigured"
POST /signal → configured totipotent germ  → mode: "model-required"
    boundary: "model-call-required"
    reason:  "MODEL_BASE_URL / MODEL_KEY not configured on the worker — the boundary stays honest"
POST /signal → differentiated rule miss    → mode: "escalation-failed", ok: false
    hint: "differentiated cell missed its table and the ancestor bridge is unconfigured — signal NOT served"
```
Nothing fetched. The v0.1 contract held exactly.

## Phase 2 — the seam opened (secrets set), real model calls

**1. Model-backed signal to a totipotent cell (real glm-4.5-flash):**
```
POST /signal {to: germ (totipotent, sheet.model=glm-4.5-flash), kind: plan, payload: {q: "In one short sentence: what is myelin?"}}
→ mode: "model", ok: true
  answer: "Myelin is a fatty sheath that insulates nerve fibers, enabling rapid transmission of nerve impulses."
  model_log: glm-4.5-flash | tokens 94/273 | latency 22538 ms | cost_estimate_usd: null
```

**2. The escalation path (differentiated rule miss → germ line answers):**
```
POST /signal {to: ack-hand (differentiated, sheet.rules=[nod→ROGER])} kind: nod
→ mode: "table", response: {ack: ROGER}, cost 0            (tendency first)

POST /signal same cell, kind: trade, payload: {parity: even, bar: 42}   (table MISS)
→ mode: "escalated", ok: true
  answer: {"response":"ack","status":"received","confirmed":{"parity":"even","bar":42}}
  answered_by: {id: c_5a537431, name: germ, tier: totipotent}   ← nearest totipotent ancestor via created_from
  escalated_from: c_a5fc98e8                                     ← the cell whose table missed
  candidate_id: 1
  model_log: tokens 172/330, latency 16344 ms
```

**3. Distillation candidate recorded (the doctrine):**
```
GET /organism/seam-live-v02/candidates
→ counts: {open: 1, distilled: 0, dismissed: 0}
  cand 1 | kind: trade | payload_shape: "bar,parity" | escalated_to: c_5a537431 | status: open
```

**4. Gardener grows the table from the candidate:**
```
POST /candidates/1/resolve {status: distilled, rule: {when: {kind: trade}, respond: {ack: ack, status: received}},
                            evidence_ref: "seam-live-v02 cand-1: glm-4.5-flash germ answer via escalation, 2026-08-25",
                            gardener_verdict: "the germ line covered the trade miss twice with a stable shape — the answer becomes tendon"}
→ resolved: 1, status: distilled, rules_now: 2
  "the hole is now deterministic tissue — the next signal of that kind hits the table, cost 0"
```

**5. The loop closes:**
```
POST /signal same cell, kind: trade, payload: {parity: odd, bar: 9}
→ mode: "table", response: {ack: ack, status: received}, cost: 0, candidate: null
```
One model call in the whole cycle. The next trade is free forever (until wounded).

**6. Cost tumor watch (the cancer metric):**
```
GET /organism/seam-live-v02/health?window=50
→ serve_modes_pct: {model: 12.5, table: 37.5, escalated: 12.5, model_required: 0, error: 25}
  totipotent_serve_pct: 25 | window: 8 | cost_tumor.warning: true
  "COST TUMOR: the germ line served 25% of the last 8 signals (> 5%) — the organism is leaning on its stem cells;
   differentiate dedicated tissue or grow rule tables from distillation candidates"
```
Correctly flagged: this demo organism is young and germ-heavy. That is the metric doing its job.

**7. v0.1 data intact after migration:** unheard-duke, band-clock, cue-tokens, seamstress-eye all listed with their cells; signals table migrated with new columns (mode, model_log, escalated_from) via `migrations/2026-08-25-v0.2-model-seam.sql`.

## Honest limits

- The DeepSeek direct key in the environment is real but the account had **zero balance** (HTTP 402 surfaced honestly as `model-error`/`escalation-failed` during local testing — the boundary never guessed). The live verification used the **Z.ai coding-plan key** (`glm-4.5-flash`).
- glm-4.5-flash is a reasoning model: latencies ranged 11–30 s, and one live call honestly **timed out at the env-configured 30 s** (the hard-timeout contract fired; timeout then widened to 60 s via `MODEL_TIMEOUT_MS`, an explicit ops decision — the code default remains 20 s).
- `cost_estimate_usd` is `null` everywhere: no per-token prices configured. Tokens and latency are always logged; cost is only computed when `MODEL_PRICE_IN_PER_MTOK`/`MODEL_PRICE_OUT_PER_MTOK` are set.
- Signal-time escalation never dedifferentiates: if the lineage has no active totipotent ancestor, escalation fails honestly (wound healing is the recall path).
- Sclerotic rule misses remain scar tissue (`table-miss`, ok=0) — the v0.1 doctrine is unchanged; escalation belongs to *differentiated* tissue with forming tables.
